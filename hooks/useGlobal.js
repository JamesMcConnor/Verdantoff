// import { getCurrentInstance } from "vue";

// export default function useGetGlobalProperties() {
//     let { appContext: { app: { config: { globalProperties } } } } = getCurrentInstance()

//     return { ...globalProperties }
// }